#ifndef FILE_FILTERING_H
#define FILE_FILTERING_H

#include <string>
inline void fileFiltering(const std::string& input, const std::string& output)
{
	std::ifstream fin(input);
	std::map<std::string,int> usermap;
	std::map<std::string,int> itemmap;
}

#endif